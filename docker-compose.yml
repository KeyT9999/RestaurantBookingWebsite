version: "3.8"

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: bookeat-app
    ports:
      - "8081:8081"
    env_file:
      - .env
    # Uncomment to force a profile
    # environment:
    #   SPRING_PROFILES_ACTIVE: dev
    restart: unless-stopped 