spring:
  profiles:
    active: dev
  datasource:
    url: ${JDBC_DATABASE_URL}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
  
  thymeleaf:
    cache: false
    encoding: UTF-8
    mode: HTML
    prefix: classpath:/templates/
    suffix: .html
  
  messages:
    basename: messages
    encoding: UTF-8
    cache-duration: 3600
  
  servlet:
    multipart:
      max-file-size: 5MB
      max-request-size: 5MB

server:
  port: 8080
  
app:
  base-url: http://localhost:8080
  upload:
    dir: uploads
    max-file-size: 5MB
    max-request-size: 5MB

# Payment configuration
payment:
  momo:
    partner-code: ${MOMO_PARTNER_CODE:MOMOB5D120250831}
    access-key: ${MOMO_ACCESS_KEY:LR71jVcxE6fV8oRZ}
    secret-key: ${MOMO_SECRET_KEY:k4xQbmkX0hAzfYQlHxEg91sZeNvYsIIT}
    endpoint: ${MOMO_ENDPOINT:https://payment.momo.vn}
    return-url: ${MOMO_RETURN_URL:http://localhost:8081/payment/momo/return}
    notify-url: ${MOMO_NOTIFY_URL:http://localhost:8081/payment/api/momo/ipn}
    # POS configuration (optional)
    pos-public-key: ${MOMO_POS_PUBLIC_KEY:}
    pos-endpoint: ${MOMO_POS_ENDPOINT:}
  payos:
    client-id: ${PAYOS_CLIENT_ID:59e68d4d-6eb8-4259-93ab-b4c6c3a6af6f}
    api-key: ${PAYOS_API_KEY:9956541e-86ab-470a-a03c-6d1cc67d95b1}
    checksum-key: ${PAYOS_CHECKSUM_KEY:eb7485ce4c656e02cae0629fcce02b2933d5b2a9b0a10447c9aa0662125835cf}
    endpoint: ${PAYOS_ENDPOINT:https://api-merchant.payos.vn}
    return-url: ${PAYOS_RETURN_URL:${app.base-url}/payment/payos/return}
    cancel-url: ${PAYOS_CANCEL_URL:${app.base-url}/payment/payos/cancel}
    webhook-url: ${PAYOS_WEBHOOK_URL:${app.base-url}/payment/api/payos/webhook}

logging:
  level:
    org.hibernate.SQL: INFO
    org.springframework.security: INFO 