package com.example.booking.config;

import org.springframework.stereotype.Component;

// TODO: Cần cập nhật DataInitializer để sử dụng model mới (RestaurantProfile, RestaurantTable, RestaurantOwner)
// Tạm thời comment out để tránh lỗi compile
@Component
public class DataInitializer {

    // DataInitializer sẽ được cập nhật sau khi hoàn thành việc sửa các model khác

} 